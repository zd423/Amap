# Optional environment overrides for systemd.
# Copy values into the service file or use EnvironmentFile.

HOST=0.0.0.0
PORT=8787
PUBLIC_BASE_URL=http://your-pi:8787
GITHUB_REPO=zuo-qirun/amap-companion
# GITHUB_TOKEN=ghp_xxx
RELEASE_TAG=latest
ASSET_PATTERN=\\.apk$
